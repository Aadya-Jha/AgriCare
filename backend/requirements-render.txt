# Lightweight Flask Backend for Render Deployment
# Flask and Web Framework
Flask==3.0.0
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
Werkzeug==3.0.1

# Database
psycopg2-binary==2.9.9

# Environment and Configuration
python-dotenv==1.0.0

# Basic Data Processing (compatible versions)
numpy==1.26.4
pandas==2.1.4

# API and HTTP
requests==2.31.0

# Firebase (lightweight)
firebase-admin==6.5.0

# Utilities
python-dateutil==2.8.2
pytz==2024.1

# Build essentials
setuptools>=69.0.0
wheel>=0.42.0

# Optional: Remove heavy dependencies for basic API functionality
# opencv-python  # Removed - causes build issues
# Pillow         # Removed - causes build issues  
# tensorflow     # Removed - too heavy for free tier
# torch          # Removed - too heavy for free tier
# geopandas      # Removed - has heavy dependencies
# matplotlib     # Removed - causes build issues