# LIGHTWEIGHT Flask Backend for Render Deployment
# Heavy dependencies removed to prevent build failures on Python 3.13

# Flask and Web Framework
Flask==3.0.0
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
Werkzeug==3.0.1

# Database (PostgreSQL optional, SQLite default)
psycopg2-binary==2.9.9

# Environment and Configuration
python-dotenv==1.0.0

# Basic Data Processing (compatible versions only)
numpy==1.26.4
pandas==2.1.4

# API and HTTP
requests==2.31.0

# Firebase (lightweight)
firebase-admin==6.5.0

# Utilities
python-dateutil==2.8.2
pytz==2024.1

# Build essentials for compatibility
setuptools>=69.0.0
wheel>=0.42.0

# REMOVED FOR DEPLOYMENT (causing build failures):
# opencv-python==4.8.0.76    # Causes Python 3.13 build errors
# Pillow==10.0.0             # Causes setuptools KeyError: '__version__'
# tensorflow                 # Too heavy for free tier
# torch                      # Too heavy for free tier
# scikit-learn               # Heavy dependency
# matplotlib                 # Causes build issues
# geopandas                  # Has complex geospatial dependencies
# shapely                    # Geographic library not needed for basic API
# folium                     # Map visualization not needed
# reportlab                  # PDF generation not needed
# openpyxl                   # Excel not needed
# celery                     # Background tasks not needed
# redis                      # Not available in free tier
# cython                     # Causes compilation issues

